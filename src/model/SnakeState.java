package model;


/**
 * SnakeState contains enums which are the directions the snake can move
 * LEFT, RIGHT, UP, DOWN
 */
public enum SnakeState {
    //UP is the most logical name for the enum
    LEFT, RIGHT, @SuppressWarnings("EnumeratedConstantNamingConvention")UP, DOWN
}
